#include <array>

//! there are 3/4 main ways to represent
//! a sparse matrix. which one do we choose? 
//! all of them?
namespace solvant {
namespace base {
template <typename T, std::size_t N, std::size_t M>
class sparse_matrix {
public:
    sparse_matrix() {}

private:
};
}  // namespace base
}  // namespace solvant
